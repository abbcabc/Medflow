<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.medflow.mapper.PatientMapper">

    <resultMap id="PatientResultMap" type="org.example.medflow.entity.Patient">
        <id column="patient_id" property="patientId" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="real_name" property="realName" />
        <result column="age" property="age" />
        <result column="id_card" property="idCard" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="gender" property="gender" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <select id="findByPhone" resultMap="PatientResultMap">
        SELECT patient_id, username, password, real_name, age, id_card, phone, email, gender, created_at
        FROM patient
        WHERE phone = #{phone}
    </select>

    <select id="findByPatientId" resultMap="PatientResultMap">
        SELECT patient_id, username, password, real_name, age, id_card, phone, email, gender, created_at
        FROM patient
        WHERE patient_id = #{patientId}
    </select>

    <insert id="insert" parameterType="org.example.medflow.entity.Patient"
            useGeneratedKeys="true" keyProperty="patientId">
        INSERT INTO patient
            (phone, password, created_at)
        VALUES
            (#{phone}, #{password}, NOW())
    </insert>

    <update id="update" parameterType="org.example.medflow.entity.Patient">
        UPDATE patient
        SET
            username = #{username},
            real_name = #{realName},
            age = #{age},
            id_card = #{idCard},
            email = #{email},
            gender = #{gender}
        WHERE patient_id = #{patientId}
    </update>

    <select id="countByPhone" resultType="int">
        SELECT COUNT(*) FROM patient WHERE phone = #{phone}
    </select>

    <update id="updatePassword" parameterType="org.example.medflow.entity.Patient">
        UPDATE patient
        SET password = #{password}
        WHERE patient_id = #{patientId}
    </update>

</mapper>